import { ScrollToTopButton, Tabs } from '../components';

const PartMPage = () => {
    return (
        <div>
            <Tabs />
            <ScrollToTopButton />
        </div>
    );
};

export { PartMPage };
